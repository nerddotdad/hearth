name: hearth-secrets
version: 0.1.1
description: >-
  SecretSource (not a tool plugin): injects Hearth AIOps secrets into process
  env (e.g. JELLYFIN_API_TOKEN) via loopback export. Check env vars / printenv,
  not a plugins tool list.
